language: python

python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "pypy"

install:
  - pip install -q --use-mirrors fixtures extras python-mimeparse manuel
  - python setup.py -q install

script:
  - make check
